# 浮水印來源

使用 CCU.jpg 作為論文浮水印。
原檔為 255 × 233 像素的 JPEG，保留原始淡藍色與白色背景，沒有透明通道；
直接複製使用，未重新編碼、去背、生成或重繪校徽。

thesis-config.tex 的 ThesisWatermarkFile 指向 assets/CCU.jpg。
模板採頁面置中、155 mm 寬等比例顯示，直接使用原圖色彩，不另疊加透明度。
155 mm 沿用原範例約 154.94 mm 的寬度，是可調整的預設，並非規範明定尺寸。

這個來源說明不授予校徽其他用途或再散布的額外權利。保留 format.pdf 供核對。
