# 浮水印來源

watermark.png 由本專案 format.pdf 實體第 10 頁的 Image90 原樣擷取，
使用 pypdf 的 page.images[0].data，包含來源的透明遮罩，未生成或重繪校徽。
原圖 800 × 761 像素，來源已是刷淡的淡藍色；用於重現該文件指定的論文浮水印。
來源頁面圖像區約 439.20 × 417.75 PDF 點，近乎頁面置中。
模板採 155 mm 寬，與來源約 154.94 mm 的寬度相符。

這個來源說明不授予校徽其他用途或再散布的額外權利。保留 format.pdf 供核對。
